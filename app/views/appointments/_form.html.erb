<%= simple_form_for([@artwork,@appointment]) do |f| %>
  <%= f.input :start_date, placeholder: "Start Date" %>
  <%= f.input :end_date, placeholder: "End Dates" %>
  <%= f.button :submit, class: "btn btn-primary"  %>
<% end %>
